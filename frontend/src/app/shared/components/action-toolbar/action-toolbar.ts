import { Component } from "@angular/core";

/**
 * Barra de filtros/acciones: contenedor blanco que agrupa selectores o
 * controles por encima de una tabla o sección. Apila en móvil y alinea en
 * fila (con base inferior) en pantallas medianas. Reemplaza el grid de filtros
 * suelto repetido en Pagos y Reportes.
 */
@Component({
  selector: "app-action-toolbar",
  templateUrl: "./action-toolbar.html",
})
export class ActionToolbar {}
